 CREATE TABLE if not exists Kunde(
                id integer primary key, +
                navn varchar(70), +
                epost varchar(50), +
                tlfNr varchar(8) +
                );